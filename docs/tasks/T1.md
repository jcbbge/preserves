# Task T1: Dashboard.tsx SolidJS optimization

## Goal
App has been refactored to apply SolidJS patterns, remove dead code, optimize reactivity, and improve performance. Need to start the app and test it thoroughly. Breaking changes may have occurred as we updated architecture and patterns of the application.

## Requirements
- Index loads with stock polaroids and login form
- default settings for polaroids saved to local storage
- refresh uses local storage settings
- Login Form submits data to Peach api.
- Login Form connects to Peach api and responds with user data
- Peach User data stored correctly in Context
- Dashboard page loads with Peach Post image data
- Dashboard settings saved to local storage

## Files to Modify
- `src/routes/index.tsx` - main entry point into application. has login component to connect users peach account. loads stock image polaroids from image data stored in repository.
- `src/routes/dashboard.tsx` - Route protected user dashboard. loads users peach post image data and displays as polaroids.

## Implementation Notes
- start application using 'npm run dev', see what is broken.

## Done When
- [ ] all requirements from above are met.

## Notes
- both routes are essentially identical, with the exception of the data they load and display.
